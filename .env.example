# Local dev uses SQLite. For Postgres/Supabase, switch the provider in
# prisma/schema.prisma and put the connection string here.
DATABASE_URL="file:./dev.db"

# Reserved for signed artifacts; sessions are DB-backed and do not need it yet.
SESSION_SECRET="change-me"
